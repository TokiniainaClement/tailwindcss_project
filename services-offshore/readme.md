# Afficher le projet dans le navigateur

- Dans VS Code ouvrir un terminal
- Dans le terminal tapez la commande:
    npx @tailwindcss/cli -i ./src/css/input.css -o ./src/css/output.css --watch

- Vous pouvez ouvrir le fichier index.html dans votre Navigateur
